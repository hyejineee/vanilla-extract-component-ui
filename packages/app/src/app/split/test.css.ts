import { createTheme, createThemeSprinkles } from "ui/system";

export const theme = createTheme({
  color: {
    bg: {
      error: {
        default: "red.300",
      },
    },
  },
});
export const sprinkles = createThemeSprinkles({
  color: {
    bg: {
      error: {
        default: "red.300",
      },
    },
  },
});
